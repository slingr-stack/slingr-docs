params:
  - name: record
    type: sys.data.Record
    description: |
      The record where the action will be executed.
  - name: interaction
    type: sys.data.Interaction
    description: |
      Interaction object to access parameters of the interaction.
returnType: null
returnDescription: null
samples:
  - title: after interaction is executed log something
    code: |
      sys.logs.info("Interaction: [" + interaction.name() + "] executed")

